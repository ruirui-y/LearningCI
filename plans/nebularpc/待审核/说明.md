# 待审核

从 ChatGPT 导回的节点细化结果会先放在这里。

LearningCI 会先检查：

- Node / Title 是否与冻结路线一致；
- 叶子任务结构是否完整；
- 固定试卷是否严格为 15 + 15 + 25 + 25 + 20 = 100；
- 是否越过本节点范围；
- 是否属于当前三节点准备窗口。

只有用户确认后，才会替换 `节点执行包/<Node>.json`；旧版本自动进入 `节点执行包/历史版本/`。
