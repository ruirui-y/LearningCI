# LearningCI 开发路线

## v0.3.0 — 人工 ChatGPT 桥接 + 三节点滚动细化

已完成：

- UI 关键字段中文化；
- 文档/说明文件尽量中文命名；
- 完全删除 DeepSeek / 模型 API 路线；
- 节点准备页面；
- 三节点准备窗口；
- 导出节点细化 ZIP；
- ChatGPT 细化结果导回；
- 本地结构与路线边界校验；
- 导入前显示任务数量/任务组变化并要求确认；
- 旧执行包自动备份；
- 草稿 → 已审核 → 已冻结 状态机；
- SQLite 跨设备一致性快照；
- NebulaRPC 总计划改为求职证据导向并冻结主线。

## v0.3.x — 真实使用反馈

只修实际使用中暴露的问题：

- 任务树展开/收起体验；
- 单任务打卡速度；
- 证据录入是否过重；
- 正式试卷回答区布局；
- 节点细化 ZIP 是否携带了足够项目上下文；
- 数据同步是否足够直观；
- 中文术语是否仍有难懂位置。

禁止因为“功能看起来酷”加入：

- 模型 API；
- Agent；
- RAG；
- 云账号；
- 社交/排行榜；
- 自动改学习路线。

## v0.4.0 — 工程证据半自动化

候选：

- 选择 Git commit 作为任务证据；
- 自动记录测试命令与输出；
- 导入 benchmark 原始结果；
- 关联日志/抓包/性能报告路径；
- 面试证据索引页。

原则：自动化只负责收集证据，不负责替用户判定能力。

## v0.5.0 — 求职证据总览

候选：

- 按能力节点聚合源码、测试、benchmark、ADR、故障记录；
- 按 DemandStation / MyMuduo / NebulaRPC / OmniBox 四个项目生成证据地图；
- 生成“60 秒回答 / 5 分钟深挖 / 源码追问 / 故障追问”复习视图；
- 统计哪些 CORE 节点虽然 PASS，但缺少真实工程证据。

## 长期目标

```text
冻结能力节点
→ 详细执行
→ 工程证据
→ 固定正式验收
→ 通过/未通过
→ 延迟复测
→ 稳定掌握
→ 面试证据索引
```
