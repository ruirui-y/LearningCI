# LearningCI v0.3.0 验证说明

发布前执行：

```text
python -m compileall -q learningci tools tests
python -m unittest discover -s tests -v
```

测试结果：18 项全部通过。

核心验证范围：

- 冻结路线 Hash；
- 68 个节点执行包格式；
- 草稿执行包在未产生运行证据前允许替换；
- 已冻结执行包拒绝静默变化；
- 三节点准备窗口；
- 节点细化 ZIP 导出内容；
- ChatGPT 返回 JSON 校验；
- 导回前暂存、确认后安装、旧版备份；
- 叶子任务初始化与工程证据 Gate；
- 单任务专注计时；
- 固定试卷首次未通过后继续复用；
- 主线通过/未通过推进规则；
- 可选节点不阻塞主线；
- SQLite snapshot backup / restore。

当前容器环境没有 PyQt6，因此自动化验证覆盖 Python 语法和核心业务逻辑，GUI 视觉仍需在本地 PyQt6 环境启动验收。
