# LearningCI v0.3.4 验证说明

发布前执行：

```text
python -m compileall -q learningci tools tests
python -m unittest discover -s tests -v
```

测试结果：21 项全部通过。

核心验证范围：

- 冻结路线 Hash；
- 68 个节点执行包格式；
- 草稿执行包在未产生运行证据前允许替换；
- 已冻结执行包拒绝静默变化；
- 三节点准备窗口；
- 节点细化 ZIP 导出内容；
- ChatGPT 返回 JSON 校验；
- 导回前暂存、确认后安装、旧版备份；
- 叶子任务初始化与工程证据 Gate；
- 单任务专注计时；
- 固定试卷首次未通过后继续复用；
- 主线通过/未通过推进规则；
- 可选节点不阻塞主线；
- SQLite snapshot backup / restore；
- 已存在同步快照的原地更新，不再依赖删除/重命名。

当前容器环境没有 PyQt6，因此自动化验证覆盖 Python 语法和核心业务逻辑，GUI 视觉仍需在本地 PyQt6 环境启动验收。


## v0.3.3 新增验证

- S0-01 重复总结任务不再计入有效叶子任务（41 → 30）。
- 小节评分保存、>=80 Gate 与证据修改后的评分失效。
- S0-01 固定正式试卷不再要求额外手写审计总结文档。
- Windows 同步快照更新逻辑改为 SQLite 直接写目标数据库，避免 WinError 32 删除共享锁问题。
- 同步快照生成后执行 integrity_check。
- 全量 21 项核心单元测试通过。

## v0.3.4 新增验证

- 兼容旧版 `weaknesses` 并提取任务 ID / 评分维度。
- 支持新版结构化 `issues`。
- 不属于当前小节的 task_id 不会生成错误跳转。
- 空问题列表能够正常显示。

本版本核心测试：**26 个自动测试全部通过**。
