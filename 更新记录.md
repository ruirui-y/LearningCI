## v0.3.11 - 2026-09-18

- 正式测试总分达到 70 即通过并解锁下一节点，不再要求 80 分才允许推进。
- 80 分和原五维最低门槛改为“稳定掌握”目标；单维度短板记录为薄弱点，不再硬卡主线。
- 70~79 分会显示“通过（带薄弱点）”，薄弱点留给 3D/7D/30D 复测继续验证。
- 升级时会自动检查旧数据库：以前因为 80 分/维度门槛被判失败、但总分已经 >=70 的首次正式测试，会自动转为通过，无需重新评分。
- 正式测试结果区明确显示“下一节点已解锁”。
- 小节验收规则保持原样，仍然是 >=80 分通过。

# LearningCI 更新记录

## v0.3.10 - 2026-09-17

### 正式测试整页滚动

- 正式测试/复测窗口改为一个统一的纵向滚动页面。
- 标题、学习计时、全部题目与回答框、保存/评分操作、评分结果、修正面板都在同一个窗口滚动条中。
- 移除题目区自己的滚动容器，修正面板再长也不会继续挤压答题区域。
- 移除正式测试修正面板固定 180~380px 的独立滚动区域，问题卡片按自然高度完整展开。
- FAIL 刚评分完成时自动定位到修正面板；重新打开或重新作答时回到试卷顶部。
- 自动保存、学习计时、跳转叶子任务、固定试卷和评分逻辑保持不变。

## v0.3.9 - 2026-09-17

### 正式测试 FAIL 修正闭环

- 正式测试未通过后新增“修正面板”，不再只显示总分和 FAIL。
- 每个修正点独立展示：题号、维度、原回答、Reviewer 批注、正确机制/修正方向。
- Reviewer 评分 JSON 新增结构化 `issues`：`question_id / dimension / title / detail / correction / related_task_ids / severity`。
- `related_task_ids` 可直接定位回已经完成的叶子任务证据；关闭正式测试窗口后自动跳转到对应任务。
- 重新作答同一冻结试卷时保留上一轮修正面板，只补本次暴露的问题，不要求重学整个节点。
- 兼容 v0.3.8 及更早的失败评分：没有结构化 `issues` 时，会根据未达门槛的维度和已保存 Reviewer evidence 自动生成修正卡片。
- `score_records` 新增 `issues_json`，数据库启动时自动迁移，不需要删除现有 SQLite 数据。
- 不修改冻结路线、`plan.json` 或正式试卷 V1.3 内容。

## v0.3.8 - 2026-09-17

- 历史能力审计节点正式验收彻底去重：叶子任务阶段只采集一次源码路径、函数、调用链和证据备注，正式测试不再要求重复抄写。
- `NRPC-S0-01` 正式试卷升级为 `V1.3`：Explanation/Prediction 只考机制，Diagnosis 改为具体故障场景，Transfer 只考破坏性修改后的机制迁移。
- `NRPC-S0-02` 同步升级为 `V1.2`，采用相同的“叶子采证一次、正式测试不重复采证”规则。
- 历史审计节点的 implementation 改为系统自动复核项（`requires_answer=false`）：UI 不再显示回答框，用户无需再次整理四条源码证据。
- 复制 Reviewer 评分提示词时，LearningCI 自动附带当前节点已保存的全部叶子工程证据与小节验收结果；implementation 25 分直接基于这些已有证据评分。
- Reviewer 规则明确：Explanation/Prediction/Diagnosis/Transfer 不得因为用户没有重复写源码绝对路径、行号或粘贴代码而扣分；机制错了就直接批注机制。
- 历史审计 Diagnosis 必须由试卷给出具体故障/错误改动，不再要求学习者自己挑一个问题再写一份小型审计报告。
- 未评分的 `V1.2`/旧版草稿升级后自动标记为 `SUPERSEDED`，保留历史回答但不产生 FAIL。
- 自动测试扩展到 35 项。

## v0.3.7 - 2026-09-17

- 修正历史能力审计节点的正式试卷职责边界：学习者只负责技术事实、源码证据、机制预测、故障诊断和当前机制内的技术迁移。
- `NRPC-S0-01` 正式试卷升级为 `V1.2`，不再要求学习者解释“为什么足以支持 NebulaRPC 不重做”或自行分类未来复用/恢复/重做。
- `NRPC-S0-02` 正式试卷升级为 `V1.1`，避免重复写 `GAME_RPC_AUDIT.md` 和提前设计 Async RPC。
- 历史审计节点的 implementation 统一解释为“拿真实源码与调用链证明结论”，而不是重新实现或再写一份总结。
- diagnosis 只诊断旧实现本身；transfer 使用当前机制内的陌生变化/破坏性修改，不把 Master Plan 决策压给学习者。
- 正式出卷和 Reviewer 评分提示词加入历史审计专用规则，确保后续复测也遵守同一职责边界。
- 升级前尚未评分的旧试卷草稿会标记为 `SUPERSEDED`，保留原回答供追溯但不产生 FAIL，然后自动创建新试卷。
- 自动测试扩展到 34 项。

## v0.3.4 - 2026-09-16

- 重写 Today 页小节验收结果面板：总分与四维评分独立展示。
- AI 扣分项按 1、2、3、4… 独立卡片显示，不再挤成一段文本。
- 小节问题区域增加独立纵向滚动条，长反馈不会撑坏页面。
- 每条问题显示任务 ID、评分维度、短标题和完整错误说明。
- 支持从扣分卡片一键定位回对应叶子任务并展开任务组。
- 小节评分提示词新增结构化 issues 字段，同时兼容历史 weaknesses 字符串。
- 保留旧数据库评分兼容：历史评分也会自动解析成编号问题卡片。
- 新增小节反馈结构化解析测试，测试总数提升到 26 项。

## v0.3.3 - 2026-09-16

- 修复 Windows 下生成 Git 同步快照时可能出现 `WinError 32` 的问题。
- 不再通过 `tmp -> 删除旧快照 -> rename` 替换 `sync/learningci.db`。
- 改为 SQLite Backup API 直接更新目标数据库，避免 Windows 文件删除共享锁冲突。
- 生成后立即执行 `PRAGMA integrity_check`，确认同步快照完整。
- 保留 SQLite 一致性快照机制；不会直接复制正在运行的 live DB 文件。

## v0.3.2 - 2026-09-16

- 修复叶子任务证据未自动保存的问题：输入后 450ms 自动写入 SQLite。
- 切换任务、暂停任务、结束学习、进入验收和关闭程序前强制保存当前输入，避免 UI 刷新后文字丢失。
- 新增“小节 AI 验收”：直接把当前小节已经填写的叶子任务回答、源码路径、函数名和证据备注复制给 ChatGPT 评分，不再要求用户重新写总结。
- 小节评分固定为理解准确度 35、源码证据 30、边界判断 25、覆盖完整度 10；本地 >=80 才通过。
- 小节通过后如果修改任何证据，旧评分自动失效并要求重新验收。
- 正式 Verification 现在同时要求：全部有效叶子任务完成 + 全部小节验收通过。
- `NRPC-S0-01` 隐藏 11 个早期重复的“形成审计结论/文档收束”任务，实际执行由 41 项调整为 30 个学习任务 + 5 个小节验收；保留既有学习记录，不改 plan.json 主路线。
- `NRPC-S0-01` 正式实现题不再要求额外手写 `MYMUDUO_AUDIT.md`，改为直接抽查 LearningCI 中已有的真实源码证据。
- 节点细化协议新增规则：禁止把“再写总结/再归纳到文档”作为重复叶子任务，后续节点默认采用小节证据验收。
- SQLite Schema 升级到 v4，新增小节验收历史记录。

## v0.3.0 - 2026-09-16

- 用户界面关键字段改为中文：执行包状态、固定试卷、学习状态、当前分、稳定分等。
- 文档、说明文件、Schema 和工具脚本尽量使用中文名称。
- 完全取消 DeepSeek 和所有模型 API 集成方向。
- 新增“节点准备”人工桥接：导出 ZIP → ChatGPT → 导回 JSON。
- 导出 ZIP 使用中文文件名，并自动携带冻结节点、基础执行包、NebulaRPC 总计划、前置学习结果、结构约束与求职证据背景。
- 新增三节点准备窗口：只允许细化当前和后面两个未完成主线节点。
- 草稿节点不能进入正式学习；已审核节点到达主线最前方时自动冻结。
- 导入 ChatGPT 结果时先放入“待审核”，显示任务组/叶子任务数量变化，用户确认后才正式采用。
- 覆盖前自动保存旧执行包到“历史版本”。
- 修正 v0.2.0 中“所有基础执行包启动后都像被冻结”的问题：未来草稿可在没有运行证据时合法替换。
- SQLite Schema 升级到 v3，增加执行包状态、版本与更新时间；支持从 v0.2 数据库迁移。
- NebulaRPC 总计划升级为 `v0.3-frozen`：不改变技术主路线，新增 15k+ 求职目标下的工程证据标准，以及 DemandStation / MyMuduo / NebulaRPC / OmniBox 四项目证明分工。
- 新增中文节点细化流程、求职证据策略文档。

## v0.2.0 - 2026-09-16

- 引入详细节点执行包：68 个 NebulaRPC 节点、1065 个基础叶子任务。
- `NRPC-S0-01` 人工细化为 41 个任务。
- Today 页面支持可展开任务树、单任务计时、证据字段和固定试卷预览。
- 首次正式验收未通过时复用同一张固定试卷；延迟复测换题。
- 新增 SQLite Git 快照同步。

## v0.1.1 - 2026-09-16

- 修正目标项目路径与 LearningCI/docs 的边界。
- 根据 NebulaRPC 总计划生成 68 节点路线。
- 试卷粘贴后按题生成独立回答框。
- 补充按钮悬浮、按下、焦点、禁用状态。

## v0.1.0

- 初始 PyQt6 + SQLite 桌面 MVP。
- 今日学习 / 冻结计划 / 复测 / 历史 / 想法停车场。
- 专注计时、五维评分、本地通过门槛。