# LearningCI v0.3.24

- Stage 1+ 节点细化改为 coding-first：第一批任务必须产生真实代码修改和编译/运行证据，禁止再次退化成历史源码审计。
- legacy `gate / 验收准备` 不再作为学习任务；旧执行包加载时自动移除并重算 task_count。
- `plan.json` 继续作为唯一不可静默覆盖的根路线合同；节点执行包改为可迭代子计划，即使已进入执行或已有学习记录也允许重新导出、细化和覆盖。
- 覆盖节点执行包时：未变化 task id 的学习证据保留；新增任务自动建档；定义发生变化的任务自动重新打开；旧版本文件自动备份。
- 当前执行中的节点覆盖后保持执行状态，不再因为“FROZEN”阻止继续学习。
- 节点准备页文案和状态改为“执行中（可覆盖）”，导入前显示新增/删除/修改任务 diff。
- AI 细化提示词和 ZIP 自检清单明确禁止生成 gate/验收准备，并要求 Stage 1+ 至少 60% 叶子任务产生真实工程结果。
